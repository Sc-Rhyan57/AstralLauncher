package com.astrallauncher.util

object LuaRunner {
    fun execute(script: String): Result<String> {
        return Result.success("Use o executor no overlay para rodar scripts via bridge TCP.")
    }
}
